// const variable = 6;
// const variable = -8;
const variable = 0;

if (variable < 0) {
  alert(`${variable} est negatif`);
} else if(variable === 0){
  alert(`${variable} est postif`);
}else{
  alert(`${variable} est positif`)
}
